<odoo>
    <template id="portal_my_home_menu_application" name="Portal layout : application menu entries" inherit_id="portal.portal_breadcrumbs" priority="40">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'applications'" t-attf-class="breadcrumb-item #{'active ' if not applications else ''}">
                <a t-if="application" t-attf-href="/my/application?{{ keep_query() }}">Funding Applications</a>
                <t t-else="">Funding Applications</t>
            </li>
            
            <li t-if="page_name == 'application'" t-attf-class="breadcrumb-item #{'active ' if not application else ''}">
                <a t-if="application" t-attf-href="/my/application?{{ keep_query() }}">Funding Applications</a>
                <t t-else="">Funding Applications</t>
            </li>
            <li t-if="application" class="breadcrumb-item active">
                <t t-esc="application.seq" t-if="application.seq != '/'"/>
                <t t-else=""><t t-esc="application.seq"/></t>
            </li>
        </xpath>
    </template>

    <template id="portal_my_home_application" name="Show Funding Application" inherit_id="portal.portal_my_home" customize_show="True" priority="40">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Funding Applications</t>
                <t t-set="url" t-value="'/my/application'"/>
                <t t-set="placeholder_count" t-value="'application_count'"/>
            </t>
        </xpath>
    </template>

    <template id="portal_my_applications" name="My Funding Application">
      <t t-call="portal.portal_layout">
        <t t-set="breadcrumbs_searchbar" t-value="True"/>

        <t t-call="portal.portal_searchbar">
            <t t-set="title">Funding Application</t>
        </t>
        <t t-if="not applications">
            <p>There are currently no Funding Application for your account.</p>
        </t>
        <t t-if="applications" t-call="portal.portal_table">
            <thead>
                <tr class="active">
                    <th>Sequence</th>
                    <th>Customer Name</th>
                    <th>Customer Phone Number</th>
                </tr>
            </thead>
            <tbody>
                <t t-foreach="applications" t-as="application">
                    <tr>
                        <td>
                            <a t-att-href="application.get_portal_url()" t-att-title="application.name">
                                <t t-esc="application.seq"/>
                            </a>
                        </td>
                        <td><span t-field="application.partner_id.name"/></td>
                        <td><span t-field="application.partner_id.mobile"/></td>
                    </tr>
                </t>
            </tbody>
        </t>
      </t>
    </template>

    <template id="portal_application_page" name="Funding Applications Portal Template" inherit_id="portal.portal_sidebar" primary="True">
        <xpath expr="//div[hasclass('o_portal_sidebar')]" position="inside">
            <div class="row mt16 o_portal_invoice_sidebar">
                <t t-call="portal.portal_record_sidebar">
                    <t t-set="classes" t-value="'col-lg-auto d-print-none'"/>
                    <t t-set="entries">
                        <ul class="list-group list-group-flush flex-wrap flex-row flex-lg-column">
                            <li class="list-group-item flex-grow-1">
                                <div class="o_download_pdf btn-toolbar flex-sm-nowrap">
                                    <div class="btn-group  flex-grow-1 mr-1 mb-1">
                                        <a class="btn btn-secondary btn-block o_download_btn" t-att-href="application.get_portal_url(report_type='pdf', download=True)" title="Download"><i class="fa fa-download"/> Download</a>
                                    </div>
                                    <div class="btn-group flex-grow-1 mb-1">
                                        <a class="btn btn-secondary btn-block o_print_btn o_portal_invoice_print" t-att-href="application.get_portal_url(report_type='pdf')" id="print_invoice_report" title="Print" target="_blank"><i class="fa fa-print"/> Print</a>
                                    </div>
                                </div>
                            </li>
                        </ul>
                    </t>
                </t>

                <div id="invoice_content" class="col-12 col-lg">
                    <t t-if="error or warning" t-call="account.portal_invoice_error"/>
                    <t t-if="success and (not error and not warning)" t-call="account.portal_invoice_success"/>
                    <div class="o_portal_html_view shadow p-3">
                        <div class="o_portal_html_loader text-center">
                            <i class="fa fa-circle-o-notch fa-spin fa-2x fa-fw text-black-50"></i>
                        </div>
                        <iframe id="invoice_html" class="mt8 mb8" width="100%" height="100%" frameborder="0" scrolling="no" t-att-src="application.get_portal_url(report_type='html')"/>
                    </div>
                </div>
            </div>
        </xpath>
    </template>

</odoo>
